import numpy as np
# Numpy arrays are superior to python libraries. They are faster and give more operations

array = np.array([1,2,3,4])
print(array)

array = array * 2
print(array) # In a python list rather than [2 4 6 8], it would print [1,2,3,4,1,2,3,4]